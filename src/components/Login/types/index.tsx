export interface ILogin {
    label: string | number;
    background: string;
    color: string;
  }
  
  export enum Login {
    limparVisor = 'C',
    apagarUltimoItem = '⌫',
    calcular = '=',
    multiplicar = '×',
    dividir = '÷',
    somar = '+',
    subtrair = '-',
  }